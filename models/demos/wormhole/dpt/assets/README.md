# Assets

Place a few small RGB images here for quick smoke tests.

Recommended:
- 1–2 KITTI raw frames (resized to <=1024px longest side).
- 1–2 NYU-Depth V2 frames (RGB only).

These are not committed; this folder stays empty in Git.
